/** @type {import('next').NextConfig} */
const nextConfig = {
  assetPrefix: 'social-media-card',
  reactStrictMode: true,
  swcMinify: true,
  images: {
    unoptimized: true
  }
}

module.exports = nextConfig
